# 모든 텍스트 파일 자동 정규화
* text=auto

# 코드 파일은 언제나 LF 사용 (CRLF 방지)
*.js   text eol=lf
*.ts   text eol=lf
*.tsx  text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.scss text eol=lf

# 쉘 스크립트는 LF 사용
*.sh   text eol=lf

# Windows 배치 파일은 CRLF 사용
*.cmd  text eol=crlf
*.bat  text eol=crlf

# 이진 파일은 변환 제외
*.png  binary
*.jpg  binary
*.zip  binary